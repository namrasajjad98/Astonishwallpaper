<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ShapeAppearance.Circle" parent="ShapeAppearance.MaterialComponents">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">100dp</item> <!-- Adjust the corner radius to match half of your imageView size -->
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="ShapeAppearance.Corners" parent="ShapeAppearance.MaterialComponents">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
        <!-- Adjust the corner radius as needed -->
    </style>

</resources>